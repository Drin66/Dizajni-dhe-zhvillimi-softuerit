class Ticket {
    constructor(id, seat, movieId, userId) {
        this.id = id;
        this.seat = seat;
        this.movieId = movieId;
        this.userId = userId;
    }
}

export default Ticket;
